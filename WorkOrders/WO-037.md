# WO-037 — Attribute and Correct Distant Silhouette/Horizon Pixels

- Status: **SUPERSEDED — EXECUTION EVIDENCE ACCEPTED; NO PRODUCT OR RENDERER RESULT**
- Issued ControlPlane baseline: `6f8eb47e09cbe21ea830cec3ae16adc8747b54c5`
- Accepted engineering baseline: `93f892dd0cf9834259b4cad2045ddb2ef9c53ed9`
- Predecessor evidence commit: `4f6cfc62ced645d4465f401520a1dfaca5e65ab7` (WO36 evidence only; no product change)
- Planned execution branch: `codex/wo37-distant-pixel-attribution`
- Predecessor: [WO-036](WO-036.md), accepted controlled falsification
- Issued: 2026-08-26
- Original explicit start directive: delivered through the user to the existing OpenMW worker on 2026-08-26
- Original diagnostic source: `7dc76a962fb5cd54494a41296b138badee15038a`
- Original execution/evidence result: blocked under Condition F at `e6a6a5b5f9b63b985d93068fb16feaacbb5f4737`
- Amendment 1 issued: 2026-08-27
- Amendment 1 explicit start directive: delivered directly to the existing OpenMW worker on 2026-08-27
- Amendment 1 repair: `9f5cfee61a450d628901d8bc08da1ddaf441fbfb`
- Amendment 1 execution/evidence result: blocked under Condition B at `c890fcda3178c36bc34ae2e99760c971dc9dd4ec`
- Amendment 2 issued: 2026-08-29
- Amendment 2 explicit start directive: delivered directly to the existing OpenMW worker on 2026-08-29
- Amendment 2 diagnostic source: `fbddf1f65fef8d3d56e5ee9b3511e0b72198163d`
- Amendment 2 execution/evidence result: blocked under Condition F at `7c17095ff876909e31e4c7e63702fff52b237373`
- Superseded by: [WO-038](WO-038.md) on 2026-08-29

## Final orchestrator disposition

The orchestrator accepts WO37's execution evidence but accepts no product change and no renderer conclusion.

Amendment 2's explicit-request precedence, bounded auto-first-exterior fallback, one-shot behavior, patch reconstruction, and focused/aggregate validation passed locally. Fast run `33265712820` then failed during cold OSG CMake configuration before OpenMW compilation because optional host GLib discovery resolved `/usr/local/Cellar/glib/2.88.2` and blindly read a missing `glibconfig.h`. No executable, IPA, device capture, target attribution, or renderer correction exists from that run.

The failure is a build-hermeticity boundary, not evidence for or against any silhouette/horizon hypothesis. WO37's three authorized Fast dispatches are exhausted. It is superseded rather than silently granted a fourth run. WO38 separately governs the narrow iOS OSG configuration recovery and completion of the unaccepted diagnostic capture. The accepted engineering baseline remains `93f892dd0cf9834259b4cad2045ddb2ef9c53ed9`.

## Objective

Identify the exact render boundary behind representative exterior defects, then apply a narrow correction only if the diagnostic evidence proves one:

1. abrupt near-black distant object/terrain/tree silhouettes, including objects that fade normally at a similar apparent distance while others remain black; and
2. the sharp blue horizontal band and large pale/white fog-colored uncovered region at the exterior horizon.

Treat these as distinct paths until correlated evidence proves they converge. The required distinction is:

- **drawn-pixel failure** — scene geometry covers the target pixel but uses an invalid shader/program, fog/sky sample, texture, lighting, blend, depth, cull, or uniform state; versus
- **coverage/composition failure** — no world drawable covers the target pixel, leaving scene clear color or a sky/RTT boundary visible.

Do not apply another global distance, paging, terrain-radius, fog-enable, or sky-blending change. Existing WO34–WO36 evidence has already falsified those as corrections.

## Authority and execution baseline

The original WO37 execution was explicitly started through the user and stopped at its review gate after changed GL4ES source failed to compile. The separate canonical ACTIVE bookkeeping update was missed; the direct worker-thread start directive and acknowledgement establish authorization, but the discrepancy is recorded rather than treated as precedent.

Amendment 1 was explicitly started, produced a qualified diagnostic IPA, and stopped at its review gate because the request-file trigger did not arm in two device sessions. Amendment 2 is ACTIVE. The orchestrator delivered its explicit start directive directly to the existing OpenMW worker on 2026-08-29. Do not resend it, duplicate execution, or begin any other work.

After activation:

1. fetch canonical WO37 from `origin/codex/wo29-ios-crash-isolation`;
2. preserve all unrelated worktree files;
3. create `codex/wo37-distant-pixel-attribution` from exact accepted engineering baseline `93f892dd0cf9834259b4cad2045ddb2ef9c53ed9`;
4. read only CURRENT_STATE, WO37, DEC-016, the WO36 final review/device evidence, DEC-013, and the WO33 final review/linked-route proof; and
5. prove rejected WO34 terrain code and all WO35/WO36 configuration experiments are absent from product source.

Workers may write execution source/tests and `Evidence/WO-037/` only. They may not modify CURRENT_STATE, WorkOrders, or Decisions. User input supplies device evidence only; it does not amend scope.

## Established evidence and source boundary

### Accepted runtime facts

- Camera/fog/view distance `7168` versus `16384` changes where the defects appear, not their character.
- `object_paging 0 → 1` at constant `7168` does not remove them.
- Sky blending is proven active in the retained device configuration and improves some fading, but does not remove either target.
- Accepted WO33 diagnostics prove broad OpenMW fog intent, GL4ES receipt, and program application, but they do not correlate a photographed target pixel to a specific drawable or program.
- The scene clear color recorded in the WO36 Arm C session matches the pale fog-colored background, but target-pixel depth/coverage was not sampled.

### Source-verified attribution seams

Codebase Memory reconnaissance followed by direct inspection of the pinned checked-out source establishes:

- OSG `osgUtil::RenderLeaf::render` has the final `Drawable`, model-view and projection matrices, render depth, state graph, and the immediate call to `Drawable::draw`.
- OSG `Drawable::draw` is the common drawable execution boundary before geometry-specific draw implementation.
- GL4ES `glDrawElementsCommon` realizes textures and holds the active GL4ES program/state immediately before the actual GLES draw.
- OpenMW `applyFogAtDist`/`applyFogAtPos` owns fog mixing and optional `sampleSkyColor(gl_FragCoord.xy / screenRes)` behavior.
- OpenMW's sky RTT is a bounded offscreen texture, while the scene camera separately clears to the live fog color.

The worker may choose a narrower verified seam, but any diagnostic design must join draw identity to actual GL4ES program/state and target-pixel coverage. Names or graph results alone are insufficient.

## Build and workflow budget

Maximum authorized Fast Development dispatches: **three**.

- At most one diagnostic IPA-producing run.
- At most one correction IPA-producing run, only after exact cause proof.
- One replacement dispatch is allowed only if a run fails before compiling the changed product sources because of patch-bootstrap, deterministic-test harness, cache, or infrastructure failure. Record and fix the pre-compile failure; do not use the replacement to change renderer scope.

Never duplicate an active/queued run. No Full Qualification is authorized.

## Amendment 1 — repair the native GLES readback loader and replace the failed diagnostic build

This amendment supersedes only the original replacement-run eligibility and Stop Condition F for the proven `glReadPixels` loader compile defect. It does not authorize a diagnostic redesign, renderer correction, additional product scope, dependency revision, workflow change, or Full Qualification.

### Reviewed original outcome

- Worker branch `codex/wo37-distant-pixel-attribution` is clean and pushed at evidence commit `e6a6a5b5f9b63b985d93068fb16feaacbb5f4737`; diagnostic source is `7dc76a962fb5cd54494a41296b138badee15038a`.
- The bounded one-frame, one-pixel OSG → GL4ES → native-present attribution design and its deterministic tests are accepted as execution evidence only. No diagnostic or renderer product change is accepted.
- Fast run `33002709474` passed patch bootstrap and validation, then failed while compiling changed GL4ES `drawing.c`. No OpenMW compile/link/package, IPA, executable, or device attribution exists.
- The exact failure is `LOAD_GLES_FPE(glReadPixels)`: that macro requires `fpe_glReadPixels`, which pinned GL4ES does not define.
- Codebase Memory reconnaissance followed by direct inspection of pinned `src/gl/loader.h` and `src/gl/texture_read.c` verifies that GL4ES's existing `gl4es_glReadPixels` path acquires the native function with `LOAD_GLES(glReadPixels)` and then calls `gles_glReadPixels`.
- The original replacement clause did not cover a failure during changed-source compilation, so the worker correctly stopped. One Fast dispatch has been consumed; the accepted engineering baseline remains `93f892dd0cf9834259b4cad2045ddb2ef9c53ed9`.

See the [orchestrator compile-failure review](../Evidence/WO-037/orchestrator-compile-failure-review.md).

### Authorized Amendment 1 work

After an explicit Amendment 1 start directive, the existing worker may:

1. Continue only on `codex/wo37-distant-pixel-attribution` from `e6a6a5b5f9b63b985d93068fb16feaacbb5f4737`, preserving unrelated worktree files and canonical ControlPlane records.
2. Confirm no active or queued duplicate Fast run.
3. Replace only the invalid native-readback acquisition in `patches/gl4es/0010-ios-target-pixel-attribution.patch` with pinned-source-correct loading. The expected narrow repair is `LOAD_GLES(glReadPixels)`; an equivalent existing GL4ES loader path is allowed only if direct pinned source proves it necessary.
4. Preserve the diagnostic trigger, target coordinate, draw correlation, candidate budgets, two bounded 1×1 per-draw samples, one bounded pre-swap sample, record schema, and renderer semantics unchanged.
5. Add or strengthen a deterministic validation that rejects `LOAD_GLES_FPE(glReadPixels)`/`fpe_glReadPixels`, verifies the ordinary native loader contract, and exercises the complete ordered GL4ES patch stack against pristine pinned revision `c9895df34cd466c23bc60c2bd3db3d87e98fcbe7`.
6. Run the focused WO37 tests, aggregate iOS validation, real patch-parser checks, source/static checks, and applicable hygiene checks before CI.
7. Trigger exactly **one Amendment 1 replacement Fast Development diagnostic run**. This is the second total Fast dispatch and is explicitly authorized despite the original pre-compile-only replacement condition.
8. If that run produces the intended diagnostic IPA, record its complete artifact/binary identity and resume the original Phase 3 device attribution and conditional Phase 4 correction gate without changing their causal requirements. The original one correction-IPA budget remains reserved and may be used only after exact device evidence proves a correctable boundary.

The unlisted GitHub Gist may mirror the completion report for convenience, but it does not replace the repo-backed `Evidence/WO-037/report.md` at the worker evidence commit.

### Amendment 1 stop boundary

Stop and report if the loader repair requires broader GL4ES or diagnostic changes, if validation fails, if the replacement run does not produce the intended IPA, or if runtime diagnostics regress presentation or remain insufficient. Do not start another replacement diagnostic run. Do not implement a renderer correction without the original target-attribution proof.

## Amendment 2 — replace the fragile request prerequisite with first-exterior auto-arm

This amendment supersedes only Amendment 1's exhausted runtime-diagnostic stop boundary and the requirement that a readable Files request exist before the already-implemented first-exterior capture can arm. It does not redesign target attribution, change renderer behavior, investigate Files/provider internals, or authorize a renderer correction.

### Reviewed Amendment 1 outcome

- Repair `9f5cfee61a450d628901d8bc08da1ddaf441fbfb` changed only the invalid GL4ES native-readback loader to pinned-source-correct `LOAD_GLES(glReadPixels)` and strengthened deterministic validation.
- Verified Fast run `33019021667` passed at that exact head and produced artifact `OpenMW-iOS-fast-60`, ID `9628038289`, digest `sha256:489e24849b01f88a47cfed96b69349d1c71434b325194b9a7d8bd239ce2e7259`, and IPA SHA-256 `898BA0F25BB6B1A76050FDDB0558721A50ADF2A08E1F067E5D670D6AE10EC96B`.
- The IPA installed and rendered normally. Two fresh device sessions emitted `target_request=none` and zero `r3.*` attribution records.
- Independent Files evidence proves that before the second session a 21-byte UTF-8 file named exactly `renderer-target-request.txt`, containing `wo37-black-silhouette`, was present beside the newly written `renderer-diagnostic.jsonl`.
- Direct source inspection verifies that startup derives both paths from the same Foundation Documents/OpenMW directory, reads the request exactly once, discards the Foundation error, and never retries. The later `openmw_ios_renderer_diag_arm_exterior_fog` function already supplies the bounded first-exterior arm point but refuses to arm while `sTargetRequest` is empty.
- The evidence excludes user placement as the working explanation but does not distinguish Files materialization, one-shot read timing, or a Foundation read failure. That diagnostic-only distinction is not required to attribute the renderer.
- Black silhouettes, the pale/white region, and the blue band remain unclassified. No renderer correction was attempted or accepted. The accepted engineering baseline remains `93f892dd0cf9834259b4cad2045ddb2ef9c53ed9`.

See the [Amendment 1 runtime review](../Evidence/WO-037/orchestrator-amendment1-runtime-review.md).

### Authorized Amendment 2 work

After an explicit Amendment 2 start directive, the existing worker may:

1. Continue only on `codex/wo37-distant-pixel-attribution` from `c890fcda3178c36bc34ae2e99760c971dc9dd4ec`, preserving unrelated worktree files and canonical ControlPlane records.
2. Confirm no active or queued duplicate Fast run.
3. Preserve valid request-file behavior, but make a missing, unreadable, empty, or invalid request fall back to one fixed diagnostic label such as `wo37-auto-first-exterior`. The existing first changed exterior-fog generation must then arm the same single-frame center-target capture exactly once per process.
4. Record whether activation came from a valid request or the auto-first-exterior fallback in the bounded startup/arm evidence. Do not add polling, Files-provider coordination, menu/touch triggers, delays, retries, new device storage, or unbounded logging/readback.
5. Preserve the target coordinate, OSG/GL4ES/present correlation, candidate budgets, two bounded 1×1 per-direct-draw samples, one bounded pre-swap sample, record schema, native GLES loader repair, and all renderer semantics unchanged.
6. Add deterministic tests that prove the fallback is independent of request-file readability, arms only at the first eligible exterior fog generation, completes at most once per process, and leaves a valid explicit request authoritative. Repeat the focused WO33/WO37 suite, aggregate iOS validation, pristine ordered OpenMW/OSG/GL4ES patch checks, real parser checks, and applicable hygiene checks.
7. Trigger exactly **one final Fast Development diagnostic run**. It is the third total WO37 dispatch and reallocates the previously reserved correction slot; no further diagnostic or correction build remains authorized under WO37.
8. If the intended IPA is produced, record full artifact/binary identity and provide concise device steps for fresh-process captures. Each target session must load a saved exterior view already centered under the crosshair so the first exterior frame represents the intended black-silhouette or blue/white-horizon target.
9. Obtain at least one representative black-silhouette attribution and one representative blue/white-region attribution if the channel operates. Preserve screenshot/session/JSONL hashes and classify the targets independently under the original causal rules.
10. Stop for orchestrator review after device attribution. Even if an exact correctable boundary is proven, do not implement it under WO37; the orchestrator will issue a separate correction work order from the evidence.

The unlisted GitHub Gist may mirror the final report for convenience, but repo-backed `Evidence/WO-037/` at the pushed worker commit remains the durable execution record.

### Amendment 2 stop boundary

Stop without another build if validation fails, the final Fast run does not produce the intended IPA, the auto-arm still emits no target records, presentation changes, or correlation remains insufficient. Do not modify renderer behavior, debug Files/provider behavior, or consume another CI dispatch. Preserve all evidence and await orchestrator review.

## Required phases

### Phase 0 — isolation and direct verification

1. Confirm READY state and explicit start receipt.
2. Confirm exact baseline, accepted WO33 correction, clean canonical boundaries, and absence of rejected WO34 source.
3. Inspect active/queued CI before dispatch.
4. Query Codebase Memory for `RenderLeaf::render`, `Drawable::draw`, `glDrawElementsCommon`, `applyFogAtDist`, sky RTT sampling, and scene clear state.
5. Verify every relied-upon implementation fact against the exact pinned source and current ordered iOS patch stack.
6. Record graph drift or Objective-C++ gaps, but do not reindex unless direct reconstruction is otherwise impossible.

### Phase 1 — causal model and observer-only diagnostic design

Design the smallest environment-gated diagnostic that can correlate a saved target view with both OSG and GL4ES without changing rendered output.

For a screen-center/crosshair target, the capture must record or prove, where applicable:

1. session/frame/draw correlation identifiers that survive OSG → GL4ES;
2. drawable and nearest useful parent/node identity, without relying on a hard-coded asset;
3. object-space bounds plus projected screen/NDC coverage, eye depth/distance, and render bin/order;
4. bound diffuse texture identity/hash when available;
5. realized GL4ES program identity and whether the fragment path contains/uses fog and sky sampling;
6. fog start/end/color/scale, far distance, sky-blending start, screen resolution, sky sampler unit/binding, and relevant program uniform locations/values;
7. blend, alpha, depth-test/write/function, cull, and material/lighting state needed to distinguish a black output from expected fog/sky mixing;
8. target-pixel final color and depth after scene rendering; and
9. scene clear color plus the sky RTT color sampled at the same normalized screen coordinate, if the path uses sky blending.

The diagnostic must explicitly classify:

- no drawable covered the target pixel / clear or sky composition exposed;
- one or more candidate drawables covered it, with the winning/nearest draw identified; or
- attribution remained ambiguous.

Use saved-camera first exterior frames, a compact ring/filter, or another source-verified trigger so startup traffic cannot exhaust the budget before the target. A diagnostic-only request file or lifecycle trigger is allowed if environment-gated and documented. Do not modify touch controls, Menu behavior, HUD layout, or gameplay input to trigger capture.

Do not read back the full framebuffer, dump full shaders, log every draw indefinitely, or exceed a compact bounded evidence file. Do not mutate GL state to obtain evidence.

### Phase 2 — deterministic validation and diagnostic build

Before the diagnostic build:

1. add deterministic tests/fixtures for target selection, projection/coverage math, cross-layer draw correlation, depth/color classification, budgets, and inactive-by-default behavior;
2. test edge cases: no draw, multiple overlapping draws, behind-camera/near-plane bounds, missing names/textures, non-target cameras, and render-list/replay behavior;
3. prove diagnostics cannot enable/disable fog, blending, depth, cull, shaders, textures, or draws;
4. reconstruct ordered OpenMW, OSG, and GL4ES patches from pristine pinned inputs with production parsers;
5. run focused and aggregate iOS validation; and
6. produce an exact diff against `93f892dd0cf9834259b4cad2045ddb2ef9c53ed9`.

Dispatch the diagnostic Fast run and verify exact run, artifact, IPA, executable, and Mach-O identities.

### Phase 3 — physical-device target captures

Use the restored `7168` Arm A camera/terrain configuration. Keep the user's retained `sky blending = true` and `sky blending start = 0.8` constant. Install the exact diagnostic IPA and enable only the established renderer-diagnostic environment/configuration path.

Capture fresh, separately identifiable sessions for:

1. a hard black silhouette/cap centered under the crosshair;
2. a nearby comparison object at similar apparent distance that fades normally, if reproducible;
3. the pale/white uncovered region centered under the crosshair; and
4. the sharp blue band centered under the crosshair.

For each session, preserve the screenshot hash, diagnostic hash/session, exact normalized target coordinate, current view/fog distance, and the compact correlated records. The worker may provide exact device steps and small diagnostic request files. It may not reinterpret visual feedback as authority to change implementation scope.

Classify black silhouettes and blue/white pixels independently. Stop before correction if target attribution is ambiguous, the diagnostic altered presentation, or the exact output boundary is not proven.

### Phase 4 — choose exactly one route

#### Route A — drawn-pixel state/program cause proven

If a target pixel is covered by identified geometry and evidence proves an invalid program, fog/sky sample, uniform, texture, lighting, blend, depth, or cull transition:

1. state the causal chain from drawable through GL4ES to final pixel;
2. implement only the narrow correction at the owning layer;
3. do not hard-code device coordinates, program IDs, texture IDs, model names, hashes, or the captured weather color;
4. add deterministic regression tests that fail on the proven transition and pass on the correction; and
5. use the authorized correction build.

#### Route B — coverage/clear/sky composition cause proven

If the target pixel has clear depth/no world coverage or is proven to come from a mismatched scene-clear/sky boundary:

1. prove the absence/ownership rather than inferring it from color alone;
2. identify the exact camera/sky/terrain composition boundary and why ordinary desktop behavior is not reproduced on this iOS path;
3. implement only a source-general correction that follows current view, fog, sky, and viewport state;
4. do not synthesize distant geometry, raise view distance, force distant terrain, or paint over the screen with a captured color; and
5. use the authorized correction build only when the causal prediction is deterministic.

#### Route C — paths diverge or only one cause is proven

A correction for one target is allowed as a valid partial result if its cause is exact and the other target remains explicitly deferred. Do not force one mechanism to explain both.

#### Route D — attribution insufficient

Make no renderer correction. Preserve the diagnostic evidence and stop for orchestrator review with the missing boundary stated precisely.

### Phase 5 — correction validation and device qualification

Before the correction build, repeat pristine patch reconstruction and all focused/aggregate tests, prove observer-only diagnostics remain gated, and record the exact correction diff.

Install the exact correction IPA and reproduce the same target views/settings. A full or partial success requires:

- the corrected target no longer exhibits the proven failure mode;
- ordinary nearby terrain, objects, water, sky, HUD, and touch overlay remain intact;
- accepted WO33 smoke, foliage, and one spell-effect path remain intact;
- Menu short-tap and long-press editor behavior do not regress;
- brief exterior traversal does not crash or jetsam; and
- any performance/hitching change is reported without claiming a benchmark.

If the correction build does not materially change its exact predicted target, revert the candidate, preserve evidence, and stop. Do not spend another build speculating.

## Prohibited changes

- Product defaults for view distance, distant terrain, object paging, sky blending, fog mode/start/end/color, FOV, near plane, terrain LOD/radius, active grid, render scale, or water.
- Rejected WO34 terrain-radius source.
- Touch layout/editor behavior, menu scrolling, lifecycle, signing, data paths, dependency revisions, or unrelated UI work.
- Renderer replacement, Metal migration, broad OpenMW/OSG/GL4ES redesign, hard-coded captured identities, unbounded logging/readback, or diagnostic state mutation.
- More than the authorized Fast dispatch budget or any Full Qualification.
- Worker changes to CURRENT_STATE, WorkOrders, or Decisions.

If a prohibited subsystem is required, stop for orchestrator amendment.

## Acceptance criteria

WO37 succeeds fully when:

1. representative black-silhouette and blue/white target pixels are each causally classified;
2. OSG drawable/coverage evidence is correlated to actual GL4ES program/state and final pixel depth/color;
3. the exact invalid boundary is corrected without a global distance/paging/fog workaround;
4. matching physical-device views materially pass;
5. established nearby rendering, touch, smoke, foliage, spell, and runtime behavior do not regress; and
6. exact commits, tests, CI, artifacts, binaries, and device evidence are preserved.

A valid partial success fixes one independently proven path and leaves the other precisely classified/deferred. A valid controlled stop proves the target is uncovered clear/sky composition even if the required correction exceeds this order. Broad attribution without target correlation is not sufficient.

## Stop conditions

- **A — authorization/baseline conflict**
- **B — target/draw/pixel correlation insufficient**
- **C — diagnostic prediction falsified or target unchanged**
- **D — broader/prohibited correction required**
- **E — diagnostic or correction runtime regression**
- **F — patch/build/infrastructure replacement budget exhausted**
- **G — correction build budget exhausted**
- **H — qualified full or partial success**

## Evidence required

Write compact records under `Evidence/WO-037/`:

- `manifest.md`
- `report.md`
- `source-and-attribution-model.md`
- `diagnostic-design-and-tests.md`
- `target-pixel-captures.md`
- bounded correlated extracts and raw-file hashes
- exact source/diagnostic/correction commits and diffs
- CI runs, artifact IDs/digests, IPA/executable hashes, Mach-O UUIDs
- screenshot identities and final device classification

Do not commit raw IPAs, large screenshots, full framebuffer dumps, full shader dumps, or redundant JSONL.

Completion message:

> WO-037 execution complete / blocked.
> Execution report: `Evidence/WO-037/report.md`
> Execution commit: `<commit>`
> Artifact / CI: `<references>`
> Stop condition: `<condition>`
> Awaiting orchestrator review.

Then stop.

## References

- [Current state](../Documentation/CURRENT_STATE.md)
- [DEC-016](../Decisions/DEC-016.md)
- [WO36 final review](../Evidence/WO-036/orchestrator-final-review.md)
- [WO36 device capture](../Evidence/WO-036/device-capture.md)
- [DEC-013](../Decisions/DEC-013.md)
- [WO33 final review](../Evidence/WO-033/orchestrator-final-review.md)
- [WO33 linked-route proof](../Evidence/WO-033/linked-route-proof.md)
