# WO-038 — Recover Cold OSG Configuration and Complete Distant Pixel Attribution

- Status: **READY — AMENDMENT 1**
- Issued ControlPlane baseline: `8a37625cf6acea6c02f1fbcc2a26e5cd89567571`
- Accepted engineering baseline: `93f892dd0cf9834259b4cad2045ddb2ef9c53ed9`
- Diagnostic continuation baseline: `7c17095ff876909e31e4c7e63702fff52b237373` (evidence only; not an accepted product baseline)
- Diagnostic source candidate: `fbddf1f65fef8d3d56e5ee9b3511e0b72198163d` (unaccepted until qualified)
- Planned execution branch: `codex/wo38-cold-osg-attribution`
- Predecessor: [WO-037](WO-037.md), superseded after its Fast budget was exhausted
- Issued: 2026-08-29
- Explicit start directive: delivered directly to the existing OpenMW worker on 2026-08-29
- Original execution source/build commit: `f3ccd53e1fa823e583057afd15e8a5ce94d0ab97`
- Original execution/evidence result: blocked under Conditions A/D at `6f3eb24ae9be73950c3813964755169497bcc26e`
- Original qualified Fast run/artifact: `33269657425`; `OpenMW-iOS-fast-66` (ID `9720624645`)
- Amendment 1 issued: 2026-08-30

## Objective

Recover the reproducible iOS cold-build path from the proven optional-host-GLib configuration failure, produce one qualified diagnostic IPA, and complete exact target-pixel attribution for:

1. abrupt near-black distant silhouettes; and
2. the sharp blue band / pale-white uncovered exterior horizon.

Treat the two targets as distinct until correlated evidence proves convergence. This order does **not** authorize a renderer correction. A proven correction requires a later canonical work order.

## Authority and execution baseline

The original WO38 execution was explicitly started and stopped at the orchestrator-review gate after producing a qualified diagnostic IPA but before trustworthy target capture. Amendment 1 is READY but not ACTIVE until the orchestrator sends its explicit continuation directive. Reading this amendment does not authorize continuation.

The original execution correctly began from WO37 evidence commit `7c17095ff876909e31e4c7e63702fff52b237373` on `codex/wo38-cold-osg-attribution`. Amendment 1 continuation instead follows its exact continuation baseline and authority below. The worker must still fetch the canonical order, preserve unrelated files, verify accepted product baseline `93f892dd0cf9834259b4cad2045ddb2ef9c53ed9` remains the source ancestor, and read only CURRENT_STATE, WO38, its Amendment 1 review, DEC-016, and directly required evidence.

Workers may modify execution source/tests, CI/build configuration within scope, and `Evidence/WO-038/`. They may not modify CURRENT_STATE, WorkOrders, or Decisions. User observations are evidence, not authority to amend scope.

## Established cause and constraints

Fast run `33265712820` passed patch bootstrap and source validation, then failed during cold OSG CMake configuration. A missed incremental build-state cache exposed optional host GLib discovery. Pinned OSG's non-required `FIND_PACKAGE(GLIB COMPONENTS gobject)` resolved `/usr/local/Cellar/glib/2.88.2`, and `FindGLIB.cmake` attempted to read an absent `glibconfig.h`.

Direct source inspection establishes:

- pinned OSG documents and supports `CMAKE_DISABLE_FIND_PACKAGE_<Package>`;
- the iOS build does not currently disable optional GLib discovery;
- the required static iOS OSG plugin set is BMP, DDS, FreeType, JPEG, PNG, OSG serializers, and TGA; and
- no required iOS plugin in that set depends on GLib.

The authorized recovery candidate is therefore the iOS-only OSG configuration argument:

`-DCMAKE_DISABLE_FIND_PACKAGE_GLIB=ON`

An equally narrow spelling/location is allowed only if direct inspection proves the same CMake effect. Do not install/upgrade Homebrew GLib, hard-code a host path, change dependency revisions, delete caches, manipulate cache keys, or disable unrelated optional packages.

The reviewed WO37 diagnostic candidate preserves valid explicit target requests and otherwise auto-arms the same bounded, one-shot capture at first eligible exterior fog generation. It is observer-only and remains unaccepted until a qualified IPA and device run validate it.

## Amendment 1 — restore Pause short-tap as a capture prerequisite

This amendment supersedes only WO38's touch prohibition, original capture-preparation stop, and build budget for the exact Pause/Menu short-tap defect described below. It does not authorize general touch work, diagnostic redesign, renderer correction, configuration changes, or Full Qualification.

### Reviewed original outcome

- Commit `f3ccd53e1fa823e583057afd15e8a5ce94d0ab97` adds only the authorized iOS OSG `-DCMAKE_DISABLE_FIND_PACKAGE_GLIB=ON` configuration and deterministic validation.
- Fast run `33269657425` passed the cold OSG configure, real OpenMW compile/link, bundle validation, packaging, and artifact upload with all relevant caches missed.
- Qualified artifact `OpenMW-iOS-fast-66` (ID `9720624645`) has digest `sha256:17b27b5a0b4a957dafefb9792f545b0fed8826955b7ebdbb9c5f878148f9d8ed` and IPA SHA-256 `d66f5c31a50a48a8a35c8b5e9bc74e558d224c2da9d0debf8072f8b61df194ee`.
- No renderer or diagnostic-capture conclusion is accepted. The physical capture did not begin because the user could not short-tap Pause/Menu to create the save required at an exact prepared viewpoint. Long-press still enters the touch editor.
- The user confirmed on 2026-08-30 that the save at the exact target location is required by the build's capture protocol and cannot be prepared without working Pause.

### Source-verified cause boundary

Codebase Memory located the OpenMW/SDL controller path; direct inspection of the current checked-out Objective-C++ and pinned source establishes:

1. ordinary touch buttons press in `touchesBegan` and release in `finishTouches`, spanning separate UIKit callbacks;
2. Pause long-press arbitration defers its short-tap action until `finishTouches`, then calls `setAction(Pause, true)` immediately followed by `setAction(Pause, false)` in the same callback;
3. pinned SDL `SDL_JoystickSetVirtualButtonInner` only writes the latest value into `hwdata->buttons[button]`; and
4. pinned SDL later emits the controller transition from that stored value in `VIRTUAL_JoystickUpdate`, after which OpenMW forwards pressed/released controller events to its input binder.

The current same-callback true/false pair can therefore collapse to the final released state before SDL/OpenMW observes the pressed edge. The authorized fix must preserve one START press through an SDL input-update opportunity and then deliver exactly one release, without leaking Pause from the long-press editor gesture.

See the [Amendment 1 orchestrator review](../Evidence/WO-038/orchestrator-amendment1-review.md).

### Authorized Amendment 1 work

After an explicit Amendment 1 continuation directive, the existing worker may:

1. Continue only on `codex/wo38-cold-osg-attribution` from clean pushed evidence commit `6f3eb24ae9be73950c3813964755169497bcc26e`, preserving canonical files and unrelated worktree state.
2. Verify no active or queued duplicate Fast run and revalidate the reviewed cause against actual checked-out `openmw_ios_touch_controls.mm`, pinned SDL, and pinned OpenMW.
3. Modify only the Pause short-tap arbitration/lifetime in `ios/openmw_ios_touch_controls.mm`, plus a small touch-model helper only if deterministic executable testing genuinely requires it. Relevant focused tests and compact `Evidence/WO-038/` records are allowed.
4. Ensure a completed short tap produces exactly one Pause/START pressed edge that survives until SDL can observe it, followed by exactly one release. The implementation must be bounded, main-thread safe, cancellation safe, and unable to leave START stuck.
5. Preserve the 0.75-second long-press threshold, editor entry, input suppression, movement cancellation, touch ownership, layout, resize/move behavior, opacity, persistence, GUI pass-through, every other action mapping, and ordinary runtime behavior.
6. Add deterministic regression coverage for short-tap press lifetime, one release, cancellation/cleanup, and long-press non-leakage. Static string assertions alone are insufficient to establish the transition lifetime; use an executable state/helper test or another deterministic mechanism that proves the ordering contract.
7. Run focused WO30/WO38 tests, aggregate validation, patch reconstruction, applicable syntax/compile checks, and diff hygiene before CI.
8. Trigger exactly **one Amendment 1 Fast Development build** containing the already-qualified diagnostic/cold-build recovery plus the narrow Pause correction. This is a new prerequisite-build slot explicitly authorized beyond the original WO38 budget. No replacement run is authorized.
9. On the exact amendment IPA, verify on device: short tap opens Pause reliably; long press enters the editor without opening Pause; cancellation/movement does not open Pause or stick START; and nearby movement/look/other touch controls remain intact.
10. Use working Pause to create the required save at the exact prepared silhouette/horizon viewpoint, then resume the original bounded fresh-process target attribution with the same IPA.
11. Stop after attribution and report. Do not implement a renderer correction under WO38.

### Amendment 1 stop boundary

Stop before build if the fix requires changing SDL, OpenMW input semantics, the long-press/editor contract, touch layout/profile behavior, diagnostic trigger semantics, or any broader subsystem. Stop after the single amendment build if it fails, Pause remains unreliable, long-press leaks Pause, another touch path regresses, or target capture remains unusable. Preserve evidence and request orchestrator review; do not dispatch a replacement.

## Scope and phases

### Phase 0 — baseline and source proof (completed; revalidate Amendment 1 continuation only)

The original execution recorded and passed:

- record branch, HEAD, ancestry, status, and relevant diffs;
- prove canonical ControlPlane files are untouched;
- verify the reported OSG `FindGLIB.cmake`, iOS configure command, and required plugin set against checked-out source; and
- prove the rejected WO34 terrain candidate and WO35/WO36 configuration experiments are absent from product source.

Use Codebase Memory only where useful for renderer structure and verify facts against source. CMake/shell configuration may be inspected directly because it is outside reliable code-graph coverage.

### Phase 1 — narrow cold-configuration recovery (completed)

Commit `f3ccd53e1fa823e583057afd15e8a5ce94d0ab97` added only the iOS OSG configure argument that disables optional GLib package discovery and deterministic regression coverage verifying:

- the iOS OSG configure command disables GLib discovery;
- no host Homebrew GLib path is required or hard-coded;
- the required iOS OSG static plugin list remains present; and
- the change does not alter dependency pins, platform target, architecture, renderer settings, or runtime defaults.

The focused WO37/WO38 validations, aggregate validation, ordered patch reconstruction, applicable syntax checks, and diff checks were recorded. Amendment 1 must preserve this recovery unchanged and rerun relevant checks before its build.

### Phase 2 — qualified diagnostic build (completed)

Fast run `33269657425` passed and produced the recorded diagnostic artifact after traversing the previously failing cold path. Its conditional replacement eligibility is expired. Amendment 1's separate one-build budget is governed only by the amendment and Build budget sections above.

### Phase 3 — Amendment 1 Pause qualification and physical-device target attribution

Only after the exact Amendment 1 IPA passes its required Pause short/long-press device checks:

1. provide concise install and capture instructions;
2. preserve valid explicit request behavior, but allow the reviewed auto-first-exterior fallback when the request is missing or invalid;
3. run fresh-process sessions with the intended target centered before the first eligible exterior capture, using reproducible save/view identities where practical;
4. collect at least one representative black-silhouette target and one blue/white-horizon target if both can be captured without changing code;
5. correlate normalized target coordinate, screenshot/session identity, OSG drawable/coverage identity, GL4ES program/state, target depth/color, sky RTT sampling, scene clear behavior, and current view/fog distance; and
6. classify each target independently as drawn-pixel failure, coverage/composition failure, or still insufficient.

The diagnostic must remain bounded, one-shot, inactive in ordinary builds, and presentation-neutral. If the auto trigger fires before the prepared exterior target or correlation is otherwise unusable, preserve the evidence and stop; do not redesign the trigger or dispatch another IPA under this order.

### Phase 4 — stop and report

Do not implement a renderer correction. State the exact proven cause boundary or the exact remaining ambiguity. Preserve compact evidence, report to the orchestrator, and stop.

## Build budget

- The original primary Fast Development dispatch was consumed successfully by run `33269657425`.
- The original conditional infrastructure replacement eligibility expired when that primary run succeeded.
- Amendment 1 authorizes exactly one additional Pause-prerequisite Fast Development dispatch.
- No replacement for the Amendment 1 dispatch.
- No renderer-correction build.
- No Full Qualification.
- Never duplicate an active or queued run.

## Prohibited changes

- Any renderer correction, including fog, sky, clear color, depth, blend, shader, terrain, object, or composition behavior.
- Product defaults for view distance, distant terrain, object paging, sky blending, FOV, near plane, render scale, terrain radius/LOD, or water.
- Installing/upgrading Homebrew GLib; hard-coding host paths; dependency pin/revision changes; broad OSG/CMake redesign; disabling unrelated packages; cache deletion or cache-key manipulation.
- Expanding diagnostic readback/logging beyond the reviewed bounded target capture or changing its target semantics.
- Touch changes except the exact Amendment 1 Pause short-tap lifetime correction. Touch layout, editor UX, opacity, persistence, menu scrolling, action mappings, lifecycle, signing, data paths, and unrelated UI remain prohibited.
- Worker changes to CURRENT_STATE, WorkOrders, or Decisions.

If any prohibited change is required, stop for orchestrator review.

## Acceptance criteria

WO38 succeeds when:

1. the iOS cold OSG configure path no longer depends on optional host GLib and the required plugin set remains intact;
2. local validation and a Fast build pass, producing a traceable IPA;
3. Pause short-tap reliably opens the pause menu while the 0.75-second long press enters the editor without leaking Pause or regressing other touch input;
4. the reviewed bounded auto/explicit trigger produces correlated target records on a physical device;
5. representative silhouette and blue/white pixels are independently classified, or one is exactly classified while the other is precisely bounded as unresolved;
6. the diagnostic does not visibly alter presentation or regress launch, exterior traversal, HUD/touch, accepted smoke/foliage/spell blending, or Menu short/long press; and
7. exact source commits, tests, CI, artifact/binary identities, screenshots, diagnostics, and hashes are preserved.

A passing build without device attribution is build recovery only, not WO38 success. A target color without draw/depth/coverage correlation is insufficient. No renderer fix may be claimed or accepted from this order.

## Stop conditions

- **A — authorization or baseline conflict**
- **B — narrow GLib-discovery recovery disproven or broader build change required**
- **C — build/infrastructure budget exhausted**
- **D — qualified IPA produced but diagnostic trigger/capture unusable**
- **E — target/draw/pixel correlation insufficient**
- **F — diagnostic runtime regression or presentation mutation**
- **G — qualified attribution success (full or partial)**

## Evidence required

Write compact records under `Evidence/WO-038/`:

- `manifest.md`
- `report.md`
- `cold-osg-configuration.md`
- `diagnostic-validation.md`
- `pause-short-tap-correction.md`
- `target-pixel-captures.md`
- exact commits/diffs and validation commands/results
- CI run, artifact identity/digest, IPA/executable hashes, Mach-O UUID
- screenshot/diagnostic identities and compact correlated extracts
- explicit independent classification for both visual targets

Do not commit IPAs, large screenshots, framebuffer dumps, full shader dumps, or redundant raw JSONL. A mirrored unlisted Gist is allowed, but repository evidence is canonical.

Completion message:

> WO-038 execution complete / blocked.  
> Execution report: `Evidence/WO-038/report.md`  
> Execution commit: `<commit>`  
> Artifact / CI: `<references>`  
> Stop condition: `<condition>`  
> Awaiting orchestrator review.

Then stop.

## References

- [Current state](../Documentation/CURRENT_STATE.md)
- [WO37 final orchestrator review](../Evidence/WO-037/orchestrator-final-review.md)
- [WO37](WO-037.md)
- [DEC-016](../Decisions/DEC-016.md)
- [WO36 final review](../Evidence/WO-036/orchestrator-final-review.md)
- [WO33 final review](../Evidence/WO-033/orchestrator-final-review.md)
