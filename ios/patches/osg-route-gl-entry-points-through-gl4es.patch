diff --git a/src/osg/GLExtensions.cpp b/src/osg/GLExtensions.cpp
index 5ecf63a..bffbc7f 100644
--- a/src/osg/GLExtensions.cpp
+++ b/src/osg/GLExtensions.cpp
@@ -56,6 +56,10 @@
     #include <dlfcn.h>
 #endif
 
+#if defined(__APPLE__) && defined(OPENMW_GL4ES_MANUAL_INIT)
+extern "C" void* gl4es_GetProcAddress(const char* name);
+#endif
+
 using namespace osg;
 
 typedef std::set<std::string>  ExtensionSet;
@@ -354,7 +358,9 @@ OSG_INIT_SINGLETON_PROXY(GLExtensionDisableStringInitializationProxy, osg::getGL
 
     #elif defined(__APPLE__)
 
-        #if defined(USE_APPLE_LEGACY_NSSYMBOL)
+        #if defined(OPENMW_GL4ES_MANUAL_INIT)
+            return gl4es_GetProcAddress(funcName);
+        #elif defined(USE_APPLE_LEGACY_NSSYMBOL)
             std::string temp( "_" );
             temp += funcName;    // Mac OS X prepends an underscore on function names
             if ( NSIsSymbolNameDefined( temp.c_str() ) )
diff --git a/include/osg/State b/include/osg/State
--- a/include/osg/State
+++ b/include/osg/State
@@ -38,6 +38,14 @@
 #include <set>
 #include <string>

+#if defined(__APPLE__) && defined(OPENMW_GL4ES_MANUAL_INIT)
+#include <cstdio>
+extern "C" void gl4es_glEnable(GLenum cap);
+extern "C" void gl4es_glDisable(GLenum cap);
+extern "C" void openmw_ios_renderer_diag_blend_event(
+    const char*, const char*, const char*) __attribute__((weak_import));
+#endif
+
 #ifndef GL_TEXTURE0
 #define GL_TEXTURE0 0x84C0
 #endif
@@ -1142,12 +1150,30 @@ class OSG_EXPORT State : public Referenced
         */
         inline bool applyMode(StateAttribute::GLMode mode,bool enabled,ModeStack& ms)
         {
+#if defined(__APPLE__) && defined(OPENMW_GL4ES_MANUAL_INIT)
+            if (mode == GL_BLEND && openmw_ios_renderer_diag_blend_event)
+            {
+                char detail[256];
+                std::snprintf(detail, sizeof(detail),
+                    "mode=%u;requested=%d;valid=%d;last=%d;issued=%d;state=%p;context=%u;route=manual-gl4es",
+                    static_cast<unsigned int>(mode), enabled ? 1 : 0, ms.valid ? 1 : 0,
+                    ms.last_applied_value ? 1 : 0,
+                    ms.valid && ms.last_applied_value != enabled ? 1 : 0,
+                    static_cast<void*>(this), getContextID());
+                openmw_ios_renderer_diag_blend_event("osg", "State::applyMode", detail);
+            }
+#endif
             if (ms.valid && ms.last_applied_value != enabled)
             {
                 ms.last_applied_value = enabled;

+#if defined(__APPLE__) && defined(OPENMW_GL4ES_MANUAL_INIT)
+                if (enabled) gl4es_glEnable(mode);
+                else gl4es_glDisable(mode);
+#else
                 if (enabled) glEnable(mode);
                 else glDisable(mode);
+#endif

                 if (_checkGLErrors==ONCE_PER_ATTRIBUTE) checkGLErrors(mode);

@@ -1163,10 +1189,27 @@ class OSG_EXPORT State : public Referenced
             {
                 if (setActiveTextureUnit(unit))
                 {
+#if defined(__APPLE__) && defined(OPENMW_GL4ES_MANUAL_INIT)
+                    if (mode == GL_BLEND && openmw_ios_renderer_diag_blend_event)
+                    {
+                        char detail[256];
+                        std::snprintf(detail, sizeof(detail),
+                            "mode=%u;requested=%d;valid=%d;last=%d;issued=1;state=%p;context=%u;unit=%u;route=manual-gl4es",
+                            static_cast<unsigned int>(mode), enabled ? 1 : 0, ms.valid ? 1 : 0,
+                            ms.last_applied_value ? 1 : 0, static_cast<void*>(this), getContextID(), unit);
+                        openmw_ios_renderer_diag_blend_event(
+                            "osg", "State::applyModeOnTexUnit", detail);
+                    }
+#endif
                     ms.last_applied_value = enabled;

+#if defined(__APPLE__) && defined(OPENMW_GL4ES_MANUAL_INIT)
+                    if (enabled) gl4es_glEnable(mode);
+                    else gl4es_glDisable(mode);
+#else
                     if (enabled) glEnable(mode);
                     else glDisable(mode);
+#endif

                     if (_checkGLErrors==ONCE_PER_ATTRIBUTE) checkGLErrors(mode);

diff --git a/src/osg/BlendFunc.cpp b/src/osg/BlendFunc.cpp
--- a/src/osg/BlendFunc.cpp
+++ b/src/osg/BlendFunc.cpp
@@ -14,6 +14,13 @@
 #include <osg/GLExtensions>
 #include <osg/State>
 #include <osg/Notify>
+#include <cstdio>

+#if defined(__APPLE__) && defined(OPENMW_GL4ES_MANUAL_INIT)
+extern "C" void gl4es_glBlendFunc(GLenum sfactor, GLenum dfactor);
+extern "C" void openmw_ios_renderer_diag_blend_event(
+    const char*, const char*, const char*) __attribute__((weak_import));
+#endif
+
 using namespace osg;

 BlendFunc::BlendFunc():
@@ -47,6 +54,20 @@ BlendFunc::~BlendFunc()

 void BlendFunc::apply(State& state) const
 {
+#if defined(__APPLE__) && defined(OPENMW_GL4ES_MANUAL_INIT)
+    if (openmw_ios_renderer_diag_blend_event)
+    {
+        char detail[256];
+        std::snprintf(detail, sizeof(detail),
+            "src_rgb=%u;dst_rgb=%u;src_alpha=%u;dst_alpha=%u;separate=%d;state=%p;context=%u;route=manual-gl4es",
+            static_cast<unsigned int>(_source_factor), static_cast<unsigned int>(_destination_factor),
+            static_cast<unsigned int>(_source_factor_alpha),
+            static_cast<unsigned int>(_destination_factor_alpha),
+            (_source_factor != _source_factor_alpha || _destination_factor != _destination_factor_alpha) ? 1 : 0,
+            static_cast<void*>(&state), state.getContextID());
+        openmw_ios_renderer_diag_blend_event("osg", "BlendFunc::apply", detail);
+    }
+#endif
     if (_source_factor != _source_factor_alpha ||
         _destination_factor != _destination_factor_alpha)
     {
@@ -62,5 +83,9 @@ void BlendFunc::apply(State& state) const
         }
     }

+#if defined(__APPLE__) && defined(OPENMW_GL4ES_MANUAL_INIT)
+    gl4es_glBlendFunc( _source_factor, _destination_factor );
+#else
     glBlendFunc( _source_factor, _destination_factor );
+#endif
 }
diff --git a/src/osg/Texture2D.cpp b/src/osg/Texture2D.cpp
--- a/src/osg/Texture2D.cpp
+++ b/src/osg/Texture2D.cpp
@@ -17,2 +17,13 @@
 #include <osg/ContextData>
+#include <cstddef>
+#include <cstdio>
+#include <cstdint>
+
+#if defined(__APPLE__) && defined(OPENMW_GL4ES_MANUAL_INIT)
+extern "C" std::uint64_t openmw_ios_renderer_diag_hash(const void*, std::size_t) __attribute__((weak_import));
+extern "C" int openmw_ios_renderer_diag_texture_category(std::uint64_t) __attribute__((weak_import));
+extern "C" void openmw_ios_renderer_diag_register_gl_texture(
+    unsigned int, std::uint64_t, int, const char*, const char*) __attribute__((weak_import));
+#endif
+
 #include <osg/Notify>
@@ -22,2 +33,27 @@
+namespace
+{
+    void registerRendererDiagnosticTexture(Texture::TextureObject* textureObject, Image* image)
+    {
+#if defined(__APPLE__) && defined(OPENMW_GL4ES_MANUAL_INIT)
+        if (!textureObject || !image || !image->data() || !openmw_ios_renderer_diag_hash
+            || !openmw_ios_renderer_diag_texture_category || !openmw_ios_renderer_diag_register_gl_texture)
+            return;
+        const std::uint64_t hash
+            = openmw_ios_renderer_diag_hash(image->data(), image->getImageSizeInBytes());
+        const int category = openmw_ios_renderer_diag_texture_category(hash);
+        if (!category)
+            return;
+        char detail[192];
+        std::snprintf(detail, sizeof(detail), "width=%d;height=%d;bytes=%u;texture=%u",
+            image->s(), image->t(), image->getImageSizeInBytes(), textureObject->id());
+        openmw_ios_renderer_diag_register_gl_texture(
+            textureObject->id(), hash, category, image->getFileName().c_str(), detail);
+#else
+        (void)textureObject;
+        (void)image;
+#endif
+    }
+}
+
 Texture2D::Texture2D():
             _textureWidth(0),
@@ -284,2 +320,4 @@ void Texture2D::apply(State& state) const
+        registerRendererDiagnosticTexture(textureObject, image.get());
+
         // unref image data?
         if (isSafeToUnrefImageData(state) && image->getDataVariance()==STATIC)
