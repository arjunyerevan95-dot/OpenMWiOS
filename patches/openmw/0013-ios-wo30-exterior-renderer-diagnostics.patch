diff --git a/apps/openmw/mwrender/renderingmanager.cpp b/apps/openmw/mwrender/renderingmanager.cpp
--- a/apps/openmw/mwrender/renderingmanager.cpp
+++ b/apps/openmw/mwrender/renderingmanager.cpp
@@ -686,6 +686,20 @@ namespace MWRender
         float fogEnd = mFog->getFogEnd(isUnderwater);
         osg::Vec4f fogColor = mFog->getFogColor(isUnderwater);
 
+#ifdef OPENMW_IOS
+        static unsigned int wo30FogSamples = 0;
+        static float wo30LastFogStart = -1.f;
+        static float wo30LastFogEnd = -1.f;
+        if (wo30FogSamples < 16 && (fogStart != wo30LastFogStart || fogEnd != wo30LastFogEnd))
+        {
+            Log(Debug::Info) << "WO30 R2 intended fog start=" << fogStart << " end=" << fogEnd << " far="
+                             << mViewDistance << " color=" << fogColor.r() << ',' << fogColor.g() << ','
+                             << fogColor.b() << " underwater=" << isUnderwater;
+            wo30LastFogStart = fogStart;
+            wo30LastFogEnd = fogEnd;
+            ++wo30FogSamples;
+        }
+#endif
         mStateUpdater->setFogStart(fogStart);
         mStateUpdater->setFogEnd(fogEnd);
         setFogColor(fogColor);
diff --git a/components/nifosg/nifloader.cpp b/components/nifosg/nifloader.cpp
--- a/components/nifosg/nifloader.cpp
+++ b/components/nifosg/nifloader.cpp
@@ -2235,6 +2235,22 @@ namespace NifOsg
                 osg::ref_ptr<osg::AlphaFunc> alphaFunc(new osg::AlphaFunc(function, threshold / 255.f));
                 alphaFunc = shareAttribute(alphaFunc);
                 node.getOrCreateStateSet()->setAttributeAndModes(alphaFunc, osg::StateAttribute::ON);
+#ifdef OPENMW_IOS
+                static unsigned int wo30AlphaStateSamples = 0;
+                if (wo30AlphaStateSamples < 64)
+                {
+                    std::string textureName = "<none>";
+                    const osg::StateAttribute* attribute
+                        = node.getOrCreateStateSet()->getTextureAttribute(0, osg::StateAttribute::TEXTURE);
+                    const osg::Texture2D* texture = dynamic_cast<const osg::Texture2D*>(attribute);
+                    if (texture && texture->getImage() && !texture->getImage()->getFileName().empty())
+                        textureName = texture->getImage()->getFileName();
+                    Log(Debug::Info) << "WO30 R1 alpha-state node=" << node.getName() << " function="
+                                     << static_cast<int>(function) << " ref=" << threshold / 255.f
+                                     << " texture=" << textureName;
+                    ++wo30AlphaStateSamples;
+                }
+#endif
             }
             else if (osg::StateSet* stateset = node.getStateSet())
             {
diff --git a/components/resource/imagemanager.cpp b/components/resource/imagemanager.cpp
--- a/components/resource/imagemanager.cpp
+++ b/components/resource/imagemanager.cpp
@@ -143,5 +143,16 @@ namespace Resource
             osg::ref_ptr<osg::Image> image = result.getImage();
 
             image->setFileName(std::string(path.value()));
+#ifdef OPENMW_IOS
+            static unsigned int wo30DdsSamples = 0;
+            if (wo30DdsSamples < 96 && ext == "dds")
+            {
+                Log(Debug::Info) << "WO30 R1 dds path=" << path << " format=" << image->getPixelFormat()
+                                 << " compressed=" << image->isCompressed()
+                                 << " translucent=" << image->isImageTranslucent() << " size=" << image->s() << 'x'
+                                 << image->t();
+                ++wo30DdsSamples;
+            }
+#endif
             if (!checkSupported(image))
             {
