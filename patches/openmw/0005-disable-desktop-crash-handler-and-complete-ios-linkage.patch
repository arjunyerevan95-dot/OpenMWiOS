diff --git a/apps/openmw/CMakeLists.txt b/apps/openmw/CMakeLists.txt
--- a/apps/openmw/CMakeLists.txt
+++ b/apps/openmw/CMakeLists.txt
@@ -171,16 +171,28 @@ if(BUILD_OPENMW)
         target_include_directories(openmw PRIVATE ${CMAKE_SOURCE_DIR}/ios)
         target_link_libraries(openmw
             "-framework Foundation"
             "-framework UIKit"
             "-framework OpenGLES"
             "-framework AudioToolbox"
             "-framework AVFoundation"
+            "-framework Accelerate"
+            "-framework CoreAudio"
+            "-framework CoreFoundation"
+            "-framework CoreGraphics"
             "-framework CoreMedia"
             "-framework CoreVideo"
             "-framework CoreMotion"
+            "-framework CoreHaptics"
             "-framework GameController"
+            "-framework Metal"
+            "-framework QuartzCore"
+            "-framework Security"
+            "-framework SystemConfiguration"
             "-framework VideoToolbox"
         )
+        # SDL's iOS backend is supplied by static Objective-C archives. Keep
+        # their classes and categories when the linker dead-strips the app.
+        target_link_options(openmw PRIVATE "-ObjC")
         if(TARGET SDL2::SDL2main)
             target_link_libraries(openmw SDL2::SDL2main)
         endif()
diff --git a/components/CMakeLists.txt b/components/CMakeLists.txt
--- a/components/CMakeLists.txt
+++ b/components/CMakeLists.txt
@@ -347,7 +347,7 @@ if(WIN32)
         windowscrashmonitor
         windowscrashshm
     )
-elseif(NOT ANDROID)
+elseif(NOT ANDROID AND NOT OPENMW_IOS)
     add_component_dir (crashcatcher
         crashcatcher
     )
diff --git a/components/debug/debugging.cpp b/components/debug/debugging.cpp
--- a/components/debug/debugging.cpp
+++ b/components/debug/debugging.cpp
@@ -17,7 +17,9 @@
 #pragma warning(pop)
 #endif
 
+#ifndef OPENMW_IOS
 #include <components/crashcatcher/crashcatcher.hpp>
+#endif
 #include <components/files/conversion.hpp>
 #include <components/misc/strings/conversion.hpp>
 #include <components/misc/strings/lower.hpp>
@@ -382,6 +384,9 @@ namespace Debug
         int ret = 0;
         try
         {
+#ifdef OPENMW_IOS
+            ret = innerApplication(argc, argv);
+#else
             if (const auto env = std::getenv("OPENMW_DISABLE_CRASH_CATCHER");
                 env == nullptr || Misc::StringUtils::toNumeric<int>(env, 0) == 0)
             {
@@ -411,6 +416,7 @@ namespace Debug
             }
             else
                 ret = innerApplication(argc, argv);
+#endif
         }
         catch (const std::exception& e)
         {
