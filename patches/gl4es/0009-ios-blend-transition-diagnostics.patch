diff --git a/src/gl/drawing.c b/src/gl/drawing.c
index 0a12ebb..9a258f1 100644
--- a/src/gl/drawing.c
+++ b/src/gl/drawing.c
@@ -286,2 +286,23 @@ GLuint len_indices(const GLushort *sindices, const GLuint *iindices, GLsizei cou
 }
+static int wo33_smoke_target_bound(void) {
+    for (int unit = 0; unit < hardext.maxtex; ++unit) {
+        gltexture_t* texture = glstate->texture.bound[unit][ENABLED_TEX2D];
+        if (texture && wo32_diag_texture_target_for_name(texture->texture) == 2)
+            return 1;
+    }
+    return 0;
+}
+
+static void wo33_diag_draw_phase(const char* site, GLenum mode, GLsizei count) {
+    if (!wo33_smoke_target_bound())
+        return;
+    char detail[384];
+    snprintf(detail, sizeof(detail),
+        "mode=%u;count=%d;blend=%d;glstate=%p;active_list=%p;list_pending=%d;list_compiling=%d;fpe_state=%p;shaderblend=%d",
+        mode, count, glstate->enable.blend ? 1 : 0, (void*)glstate,
+        (void*)glstate->list.active, glstate->list.pending, glstate->list.compiling,
+        (void*)glstate->fpe_state, globals4es.shaderblend);
+    wo33_diag_blend_event(site, detail);
+}
+
 
@@ -290,2 +311,3 @@ static void glDrawElementsCommon(GLenum mode, GLint first, GLsizei count, GLuint
         bitmap_flush();
+    wo33_diag_draw_phase("glDrawElementsCommon", mode, count);
     DBG(printf("glDrawElementsCommon(%s, %d, %d, %d, %p, %p, %d)\n", PrintEnum(mode), first, count, len, sindices, iindices, instancecount);)
@@ -597,2 +619,3 @@ AliasExport(void,glDrawRangeElements,EXT,(GLenum mode,GLuint start,GLuint end,GL
 void APIENTRY_GL4ES gl4es_glDrawElements(GLenum mode, GLsizei count, GLenum type, const GLvoid *indices) {
+    wo33_diag_draw_phase("glDrawElements.intake", mode, count);
     DBG(printf("glDrawElements(%s, %d, %s, %p), vtx=%p map=%p, pending=%d\n", PrintEnum(mode), count, PrintEnum(type), indices, (glstate->vao->vertex)?glstate->vao->vertex->data:NULL, (glstate->vao->elements)?glstate->vao->elements->data:NULL, glstate->list.pending);)
@@ -643,2 +666,3 @@ void APIENTRY_GL4ES gl4es_glDrawElements(GLenum mode, GLsizei count, GLenum type
 
+        wo33_diag_draw_phase("glDrawElements.capture", mode, count);
     if (compiling) {
@@ -675,2 +699,3 @@ void APIENTRY_GL4ES gl4es_glDrawElements(GLenum mode, GLsizei count, GLenum type
 
+        wo33_diag_draw_phase("glDrawElements.intercept", mode, count);
     if (intercept) {
@@ -705,2 +730,3 @@ AliasExport(void,glDrawElements,,(GLenum mode, GLsizei count, GLenum type, const
 void APIENTRY_GL4ES gl4es_glDrawArrays(GLenum mode, GLint first, GLsizei count) {
+    wo33_diag_draw_phase("glDrawArrays.intake", mode, count);
     DBG(printf("glDrawArrays(%s, %d, %d), list=%p pending=%d\n", PrintEnum(mode), first, count, glstate->list.active, glstate->list.pending);)
@@ -740,2 +766,3 @@ void APIENTRY_GL4ES gl4es_glDrawArrays(GLenum mode, GLint first, GLsizei count)
 
+        wo33_diag_draw_phase("glDrawArrays.capture", mode, count);
     if (glstate->list.active) {
@@ -763,2 +790,3 @@ void APIENTRY_GL4ES gl4es_glDrawArrays(GLenum mode, GLint first, GLsizei count)
 
+        wo33_diag_draw_phase("glDrawArrays.intercept", mode, count);
     if (intercept) {
diff --git a/src/gl/enable.c b/src/gl/enable.c
index d645568..d12204e 100644
--- a/src/gl/enable.c
+++ b/src/gl/enable.c
@@ -2,2 +2,3 @@
 #include "glstate.h"
+#include "ios_renderer_diag.h"
 #include "init.h"
@@ -5,2 +6,4 @@
 #include "debug.h"
+#include <stdio.h>
+
 
@@ -110,3 +113,22 @@ static void proxy_glEnable(GLenum cap, bool enable, void (APIENTRY_GLES *next)(G
         proxy_GOFPE(GL_FOG, fog, glstate->fpe_state->fog=enable);
-        case GL_BLEND: if(glstate->enable.blend != enable) {FLUSH_BEGINEND; glstate->enable.blend = enable; if(glstate->fpe_state && globals4es.shaderblend) { glstate->fpe_state->blend_enable = enable; } else next(cap);} break;
+        case GL_BLEND: {
+            const int previous = glstate->enable.blend;
+            const int changed = previous != enable;
+            if(changed) {
+                FLUSH_BEGINEND;
+                glstate->enable.blend = enable;
+                if(glstate->fpe_state && globals4es.shaderblend) {
+                    glstate->fpe_state->blend_enable = enable;
+                } else next(cap);
+            }
+            char wo33_detail[384];
+            snprintf(wo33_detail, sizeof(wo33_detail),
+                "requested=%d;previous=%d;current=%d;changed=%d;glstate=%p;active_list=%p;list_pending=%d;fpe_state=%p;shaderblend=%d;native_forwarded=%d",
+                enable ? 1 : 0, previous ? 1 : 0, glstate->enable.blend ? 1 : 0, changed,
+                (void*)glstate, (void*)glstate->list.active, glstate->list.pending,
+                (void*)glstate->fpe_state, globals4es.shaderblend,
+                changed && !(glstate->fpe_state && globals4es.shaderblend));
+            wo33_diag_blend_event("proxy_glEnable(GL_BLEND)", wo33_detail);
+            break;
+        }
         proxy_GO(GL_CULL_FACE, cull_face);
diff --git a/src/gl/ios_renderer_diag.c b/src/gl/ios_renderer_diag.c
index af9ad73..2a183fe 100644
--- a/src/gl/ios_renderer_diag.c
+++ b/src/gl/ios_renderer_diag.c
@@ -19,2 +19,4 @@ extern unsigned int openmw_ios_renderer_diag_matching_exterior_fog_generation(
     float, float, const float*) WO31_WEAK_IMPORT;
+extern void openmw_ios_renderer_diag_blend_event(
+    const char*, const char*, const char*) WO31_WEAK_IMPORT;
 extern uint64_t openmw_ios_renderer_diag_hash(const void*, size_t) WO31_WEAK_IMPORT;
@@ -134,2 +136,13 @@ int wo32_diag_texture_target_for_name(unsigned int texture)
 }
+void wo33_diag_blend_event(const char* site, const char* detail)
+{
+#if defined(__APPLE__) || defined(WO31_DIAGNOSTIC_TEST_BRIDGE)
+    if (available_core() && openmw_ios_renderer_diag_blend_event)
+        openmw_ios_renderer_diag_blend_event("gl4es", site, detail);
+#else
+    (void)site;
+    (void)detail;
+#endif
+}
+
 
diff --git a/src/gl/ios_renderer_diag.h b/src/gl/ios_renderer_diag.h
index 5bc1310..2b9ff2d 100644
--- a/src/gl/ios_renderer_diag.h
+++ b/src/gl/ios_renderer_diag.h
@@ -10,2 +10,3 @@ void wo31_diag_texture_upload(unsigned int texture, const void* data, size_t siz
 int wo31_diag_texture_category_for_name(unsigned int texture);
+void wo33_diag_blend_event(const char* site, const char* detail);
 int wo32_diag_texture_target_for_name(unsigned int texture);
diff --git a/src/gl/listdraw.c b/src/gl/listdraw.c
index d616b3b..197bb58 100644
--- a/src/gl/listdraw.c
+++ b/src/gl/listdraw.c
@@ -5,2 +5,3 @@
 #include "fpe.h"
+#include "ios_renderer_diag.h"
 #include "init.h"
@@ -12,2 +13,28 @@
 #include "fpe.h"
+#include <stdio.h>
+
+static int wo33_smoke_target_bound(void)
+{
+    for (int unit = 0; unit < hardext.maxtex; ++unit)
+    {
+        gltexture_t* texture = glstate->texture.bound[unit][ENABLED_TEX2D];
+        if (texture && wo32_diag_texture_target_for_name(texture->texture) == 2)
+            return 1;
+    }
+    return 0;
+}
+
+static void wo33_diag_replay_phase(const char* site, renderlist_t* list, GLenum mode, int count)
+{
+    if (!wo33_smoke_target_bound())
+        return;
+    char detail[384];
+    snprintf(detail, sizeof(detail),
+        "mode=%u;count=%d;blend=%d;glstate=%p;replay_list=%p;active_list=%p;list_pending=%d;list_compiling=%d;fpe_state=%p;shaderblend=%d",
+        mode, count, glstate->enable.blend ? 1 : 0, (void*)glstate, (void*)list,
+        (void*)glstate->list.active, glstate->list.pending, glstate->list.compiling,
+        (void*)glstate->fpe_state, globals4es.shaderblend);
+    wo33_diag_blend_event(site, detail);
+}
+
 
@@ -726,4 +753,7 @@ void draw_renderlist(renderlist_t *list) {
 			mode = GL_LINES;
-		if ((glstate->polygon_mode == GL_POINT) && (mode>=GL_TRIANGLES))
+        if ((glstate->polygon_mode == GL_POINT) && (mode>=GL_TRIANGLES))
 			mode = GL_POINTS;
+        wo33_diag_replay_phase("draw_renderlist.native-draw", list, mode,
+            indices ? list->ilen : list->len);
+
 
